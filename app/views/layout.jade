doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/bootstrap-darkly.css')
  body
    if messages
      each message in messages
        div(class="alert alert-dismissible alert-" + (message.type === 'error' ? 'danger' : message.type))=message.message
    block messages
    block content
